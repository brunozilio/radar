"""Finalize documentation/hashes from completed audits; no inference or source mutation."""
from pathlib import Path
import json,hashlib
P=Path(__file__).resolve().parent;ROOT=P.parents[1]
def sha(p):return hashlib.sha256(p.read_bytes()).hexdigest()
r=json.loads((P/'verification.json').read_text());t=json.loads((P/'tree-threshold-verification.json').read_text());gate=json.loads((ROOT/'outputs/experimento-radar-reservas-2021-2022-20260922/pre-inference-manifest.json').read_text());assert all(sha(ROOT/f)==h for f,h in gate['input_sha256'].items())
lines=['# Auditoria independente — primeira avaliação reservada de 2021/2022','',f"Passaram {len(r['checks'])} verificações: 24 modelos fixos, 48 aplicações, 26.340 linhas agendadas, 156 exclusões de fronteira e 432 métricas recalculadas. Os 153 hashes de entradas e as referências dos modelos, treino e helpers conferem. Nenhum treino ou ajuste foi executado.",'',f"Ordem registrada: protocolo {r['time_order']['protocol']}; entradas congeladas {r['time_order']['input_freeze']}; inferência {r['time_order']['inference_start']} até {r['time_order']['inference_finish']}. Os arquivos e a ligação entre manifestos conferem. Esta evidência local não é prova de toda computação anterior fora do escopo.",'','A matriz reconstruída independentemente reproduziu exatamente 34 aplicações. Em 14 aplicações, diferenças de chuva da ordem de 1e-13 atravessaram limiares das árvores e alteraram 203 previsões. A matriz ORIGINAL congelada reproduziu exatamente todas as previsões dessas 14 aplicações; nenhum valor foi alterado ou arredondado.','',f"O rastreamento identificou {t['tree_crossings']} divergências de caminho, todas em preditores de chuva; a soma das diferenças das folhas reproduz cada diferença de previsão até 1e-12. Maior diferença: 0,029309652 m, candidato h4, origem 29/05/2022 23h (11,945531397 m congelado versus 11,916221746 m reconstruído). Casos e limiares estão em threshold-sensitivity.csv e tree-threshold-crossings.csv. Isto limita qualquer alegação de equivalência exata sob outra implementação numérica.",'','| Período | h | Acertos controle→candidato / alvos ≥7 m | MAE (m) | Máximo (m) |','|---|---:|---:|---:|---:|']
for x in r['selected_changes']:
 if x['subset']=='level_ge_7m':lines.append(f"| {x['period']} | {x['horizon_h']} | {x['control_hits']}→{x['candidate_hits']}/{x['observed_targets']} | {x['control_mae']:.6f}→{x['candidate_mae']:.6f} | {x['control_max']:.6f}→{x['candidate_max']:.6f} |")
lines += ['', 'No recorte de cheia, o candidato ganha acertos em 7 horizontes e perde em 2 em 2021 (3 empates); em 2022 ganha em 8 e perde em 4. O resultado agregado ganha em 9 e perde em 3. O conjunto geral agregado melhora acertos nos 12 horizontes, mas isso não elimina as regressões de cheia: em 2022/h12, 147→120 acertos e MAE 1,027166→1,063000 m. Não há domínio geral ou promoção.', '', 'Todos os 11 alvos de cheia de 2021 têm previsão; em 2022 são 312 alvos e 311 pares por horizonte, com uma falha mantida no denominador. Há 323 alvos de cheia agregados, 322 pares e uma falha. Alvos desconhecidos não viram acertos. Todas as linhas têm falta em algum dos primeiros 24 campos: complete24 é vazio e missing24 coincide com full_schedule. A ausência auxiliar não criou filtro adicional.', '', 'Limites: os modelos foram ajustados com dados de 2025/26, portanto este teste mede transferência retrospectiva a janelas reservadas, não previsões que poderiam ser emitidas com somente o treino disponível em 2021/22. Amostras horárias são correlacionadas; 11/312 alvos não representam esse número de eventos independentes. Fuso, datum e disponibilidade histórica permanecem pressupostos. O teste não cumpre a meta prospectiva de 98%. Se estes resultados orientarem ajustes futuros, as janelas passam a desenvolvimento e não podem ser reapresentadas como teste novo independente.']
hits=json.loads((P/'hit-sensitivity.json').read_text());lines += ['', f"Diagnóstico de classificação: as {hits['prediction_cells']} células de previsão alteradas correspondem a {hits['unique_year_origin_pairs']} origens distintas e {hits['unique_year_target_time_pairs']} timestamps-alvo distintos; não são uma contagem de eventos. Todas têm truth finita. Nenhuma muda a classificação |erro|≤0,50 m (zero acerto→erro e zero erro→acerto), no geral ou no recorte ≥7 m, em cada família/ano/horizonte. Os 96 grupos estão em hit-sensitivity-groups.csv; os 203 casos em hit-sensitivity-cases.csv. Isto não torna a diferença automaticamente tolerável nem apaga a sensibilidade de reprodução. As métricas primárias permanecem intocadas."]
(P/'README.md').write_text('\n'.join(lines)+'\n')
manifest=[dict(file=str(f.relative_to(P)),sha256=sha(f)) for f in sorted(P.rglob('*')) if f.is_file() and f.name!='artifact-hashes.json'];(P/'artifact-hashes.json').write_text(json.dumps(manifest,indent=2)+'\n');assert all(sha(P/x['file'])==x['sha256'] for x in manifest);print({'verified_artifacts':len(manifest),'manifest_sha256':sha(P/'artifact-hashes.json')})
